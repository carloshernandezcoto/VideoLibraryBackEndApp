Security:
Before putting this application in a production environment or anywhere in the Internet, make sure to:

= Enable authentication by setting the "bypassAuth" property on default.json to false.
- Remove the following line on index.js: app.use(cors());

Local passwords:
12345